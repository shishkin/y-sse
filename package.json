{
  "name": "workspace",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "pnpm": ">=10",
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter ./examples/astro run dev",
    "check": "pnpm --recursive --if-present run check",
    "build": "pnpm --recursive --if-present run build",
    "publish": "pnpm --filter y-sse publish"
  }
}
